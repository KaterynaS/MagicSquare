<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TileStile" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:background">@color/buttonBackgroundColor</item>
        <item name="android:layout_height">@dimen/button_side_length</item>
        <item name="android:layout_width">@dimen/button_side_length</item>
        <item name="android:layout_margin">@dimen/button_margin</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="EmptyCellStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:background">@color/transparentWhite</item>
        <item name="android:layout_height">@dimen/button_side_length</item>
        <item name="android:layout_width">@dimen/button_side_length</item>
        <item name="android:layout_margin">@dimen/button_margin</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">@color/buttonBackgroundColor</item>
        <item name="android:textSize">26sp</item>
    </style>

</resources>
